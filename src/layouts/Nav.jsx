import React from "react";

export default function Nav() {
  return (
    <nav className="nav" aria-label="Global Navigation">
      <a href="#">SERVICE</a>
      <a href="#">HISTORY</a>
      <a href="#">REPORTS</a>
    </nav>
  );
}
